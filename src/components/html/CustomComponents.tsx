import Greet from "./../Greet";
const CustomComponents = ({ isLoggedIn, messageCount, name }: React.ComponentProps<typeof Greet>) => {
  return <div>Custom Components</div>;
};
export default CustomComponents;
